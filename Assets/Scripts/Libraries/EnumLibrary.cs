using UnityEngine;

public class EnumLibrary : MonoBehaviour
{
    public enum EDamageType { impact, puncture, slash, heat, cold, toxic, electric };
    public enum EWeaponType { hitscan, projectile };
}
